org: pabdelli
app: serverless-api
	
# serverless.yml
 
service: serverless-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: 'us-east-1'
  profile: 'default'

functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'
	
plugins:
  - serverless-offline
